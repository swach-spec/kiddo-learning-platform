"use client";
import { useState } from "react";
import { getCurrentPlayer, awardXP, recordActivityResult } from "@/lib/player";
export default function G2Sequencing(){const [done,setDone]=useState(false);function finish(){const p=getCurrentPlayer();if(p){awardXP(p.id,40);recordActivityResult({id:`g2-seq-${Date.now()}`,playerId:p.id,activityType:"practice_challenge",activityId:"g2-sequencing",skills:["grammar","reading"],curriculumId:"g2-sequencing",concept:"sentence sequencing",correct:true,attempts:1,hintsUsed:0,xpAwarded:40,timestamp:new Date().toISOString()})}setDone(true)}return <main className="mx-auto max-w-2xl p-6"><div className="rounded-3xl border bg-white p-8 text-center shadow-sm"><div className="text-5xl">🧩</div><h1 className="mt-3 text-3xl font-bold">Put the Story in Order</h1><p className="mt-3 text-slate-600">This activity will help you use first, next and last to put events in order.</p>{done?<><p className="mt-5 font-semibold">Great! You completed this activity.</p><a className="mt-5 inline-block rounded-2xl bg-slate-900 px-6 py-3 font-semibold text-white" href="/">Continue My Journey</a></>:<button onClick={finish} className="mt-6 rounded-2xl bg-slate-900 px-6 py-3 font-semibold text-white">Start Activity</button>}</div></main>}
